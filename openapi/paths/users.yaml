get:
  summary: Get users
  description: Returns a list of users
  operationId: findUsers
  security:
    - cookieAuth: []
    - bearerAuth: []
  tags:
    - users
  parameters:
    - name: limit
      in: query
      description: How many documents to return
      schema:
        type: integer
        minimum: 1
        maximum: 100
        default: 10
    - name: offset
      in: query
      description: How many documents to skip, useful for pagination-like behaviour.
      schema:
        type: integer
        minimum: 0
        default: 0
  responses:
    '200':
      description: Successfully returned a list of users
      content:
        application/json:
          schema:
            $ref: '../components/schemas/ArrayOfUsers.yaml'
      headers:
        Link:
          schema:
            $ref: '../components/headers/Link.yaml'
        X-Next-Page:
          schema:
            $ref: '../components/headers/X-Next-Page.yaml'
        X-Page:
          schema:
            $ref: '../components/headers/X-Page.yaml'
        X-Per-Page:
          schema:
            $ref: '../components/headers/X-Per-Page.yaml'
        X-Prev-Page:
          schema:
            $ref: '../components/headers/X-Prev-Page.yaml'
        X-Total:
          schema:
            $ref: '../components/headers/X-Total.yaml'
        X-Total-Pages:
          schema:
            $ref: '../components/headers/X-Total-Pages.yaml'
    '401':
      $ref: '../components/responses/Unauthorized.yaml'
    '403':
      $ref: '../components/responses/Forbidden.yaml'
